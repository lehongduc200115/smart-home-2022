export enum SortFieldEnum {
  STATUS = 'status',
  CREATED_AT = 'createdAt',
  UPDATED_AT = 'updatedAt'
}

export enum SortTypeEnum {
  ASCENDING = 'ASC',
  DESCENDING = 'DESC'
}

export enum Status {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE'
}
export enum RuleCondition {
  AND = 'AND',
  OR = 'OR'
}

export enum CompiledRuleCondition {
  ALL = 'all',
  ANY = 'any'
}
